# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Node (if a build step is added later)
node_modules/
dist/
*.min.js

# Temp
*.log
*.tmp

# Deploy script — local config and cached auth tokens (never commit)
scripts/deploy.config.json
.d365deploy/

# Playwright — saved browser sessions, test artifacts, and local env config (never commit)
playwright/.auth/
playwright/test.env.json
playwright-report/
test-results/

# Claude Code local infra
.claude/
